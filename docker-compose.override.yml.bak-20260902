# Local deployment overrides - NOT committed (gitignored).
# Merged automatically by `docker compose` on top of docker-compose.yml.
services:
  gemini-web2api:
    volumes:
      - ./cookie_pro.txt:/app/cookie_pro.txt:ro
    environment:
      TZ: Asia/Shanghai
    networks:
      - default
      - new-api-network

networks:
  new-api-network:
    external: true
    name: ai939593xyz_new-api-network
